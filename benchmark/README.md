# benchmark — Table 1

Nextflow traces of RiboFlow_v2 over one and three libraries, three repeats each
(`runs/`), the scenario definitions (`scenarios/`, `config/`) and `host.json`.

```bash
python benchmark/summarize_benchmarks.py --check   # every value in Table 1 (docs/numeric_claims.tsv C21–C34)
```
